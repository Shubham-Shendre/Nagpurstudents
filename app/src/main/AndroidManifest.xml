<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.nagstud.android.nagpurstudentsorg">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SyllabusActivity" />
        <activity android:name=".DownloadActivity" />
        <activity android:name=".SubscribedActivity" />
        <activity android:name=".EventsActivity" />
        <activity android:name=".AboutusActivity" />
        <activity android:name=".SingleItemView" />
        <activity android:name=".Answers_Activity1" />
        <activity android:name=".Answers_Activity2"></activity>
    </application>

</manifest>